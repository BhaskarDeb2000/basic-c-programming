#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    int i;
    for (int i = 1; i <= 1; i++)
    {
        scanf("%d", &i);
        if (i == 1)
        {
            printf("one\n");
        }
        else
        {
            printf("No one Found\n");
        }
    }
    return 0;
}